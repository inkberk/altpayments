/*
import * as React from 'react';
import * as styles from './index.scss';

// import ReactTable from 'react-table';
import 'react-table/react-table.css';
import './arrow-fix.css';

export interface TableProps {
  data: any;
  columns: any;
  nodatatext: any;
  SubComponent: any;
  hover: any;
  top_line: any;
  header: any;
}

const Table = (props: TableProps) => (

    <div className={[styles.wrapper].join(' ')}>
        {/!*<ReactTable
            sortable={false}
            showPagination = {false}
            data={data}
            columns={columns}
            minRows={0}
            noDataText={nodatatext}
            SubComponent={SubComponent}
            hover={hover}
            top_line={top_line}
            header={header}


            getProps={() => {
                return {
                    className: (styles.table_wrapper_style)
                }
            }}

            getTableProps={() => {
                return {
                    className: (styles.table_style)
                }
            }}

            getTheadProps={() => {
                return {
                    className: (styles.tr_thead_style)
                }
            }}

            getTheadThProps={() => {
                return {
                    className: (styles.td_thead_style)
                }
            }}

            getTbodyProps={() => {
                return {
                    className: (styles.tr_block_style)
                }
            }}

            getTrGroupProps={() => {
                return {
                    className: [styles.tr_style, ]
                }
            }}

            getTdProps={() => {
                return {
                    className: (styles.td_style)
                }
            }}

            getResizerProps={() => {
                return {
                    className: (styles.display_none)
                }
            }}

            getNoDataProps={() => {
                return {
                    className: (styles.no_data)
                }
            }}

            // getPaginationProps={() => {
            //     return {
            //         className: (styles.pagination)
            //     }
            // }}
        />*!/}
        {/!*<ReactPaginate*!/}
            {/!*pageCount={3}*!/}
            {/!*containerClassName="pagination"*!/}
        {/!*!/>*!/}
    </div>
);

export default Table;
*/
